#JSGF V1.0;

// https://www.w3.org/TR/jsgf

grammar numbers;

public <main> = [<wake> <command> | <others>];

<wake> = jarvis;
<command> = (ouvre|ferme) les (stores|volets|lumières);

<others> = <NULL> *;
