#JSGF V1.0;

// https://www.w3.org/TR/jsgf

grammar numbers;

public <main> = [<wake> <command> | <others>];

<wake> = jarvis;
<command> = <action> <objet>;
<action> = (ou|fer|ouvre|ferme);
<objet> = (un objet|les stores|les volets|les lumières);

<others> = <NULL> *;
